---
controller_credentials:
  - name: "CAC Creds"
    description: "Sample Machine credential created via CaC"
    organization: "RHEL Environment"
    credential_type: 1  # Machine credential type ID
    inputs:
      username: "ansible"
      password: "ansible"
      become_method: "sudo"
      become_username: "root"
      become_password: ""

