---
- name: Configure Inventory and Hosts AAP
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../env/aap_target.yml
    - configs/inventory_hosts.yml

  tasks:
    - name: Create Inventory and Hosts in Ansible Automation Platform
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch