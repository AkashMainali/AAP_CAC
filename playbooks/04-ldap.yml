---
- name: Create Teams in Ansible Automation Platform
  hosts: localhost
  gather_facts: no
  vars_files:
    - ../env/aap_target.yml
    - configs/04-ldap.yml

  tasks:
    - name: Configure LDAP for Ansible Automation Platform 2.6
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch
