---
- name: Create Organizations in Ansible Automation Platform
  hosts: localhost
  gather_facts: no
  vars_files:
    - ../env/aap_target.yml
    - configs/orgs.yml
    - configs/teams.yml
    - configs/roles.yml
  tasks:
    - name: Ensure Organizations are present in AAP 2.6
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch
