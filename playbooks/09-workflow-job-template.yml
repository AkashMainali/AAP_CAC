---
- name: Configure Workflow Job Templates
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../env/aap_target.yml
    - configs/09-workflow-job-templates.yml

  tasks:
    - name: Create Workflow and Nodes
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch