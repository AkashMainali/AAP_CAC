---
- name: Create Teams in Ansible Automation Platform
  hosts: localhost
  gather_facts: no
  vars_files:
    - ../env/aap_target.yml
    - configs/teams.yml

  tasks:
    - name: Ensure Teams are present in AAP 2.6
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch
